import React, {Component} from 'react';
import PApply from '../component/pApply';

class Papply extends Component{
    render(){
        // var currentLocation = this.props.location.pathname;
        // console.log(currentLocation);
        return(
            <PApply />
        );
    }
}

export default Papply;